{
  "name": "euystacio-framework",
  "version": "1.0.0",
  "description": "Euystacio Framework — Lex Amoris Whitelist and Nexus Core Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:optimism": "hardhat run scripts/deploy.js --network optimism",
    "deploy:optimism-sepolia": "hardhat run scripts/deploy.js --network optimism-sepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
